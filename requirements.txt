# Serving + training dependencies.
# CPU-only torch keeps the install ~200MB instead of ~2.5GB:
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
torch>=2.2
torchvision>=0.17
flask>=3.0
pillow>=10.0
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3
matplotlib>=3.7
tqdm>=4.66
